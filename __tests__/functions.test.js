import { makeBooleans, idPrimeNumbers } from './../src/js/functions.js';

describe ('makeBooleans', () => {
 
  test('should blah blah', () =>{
    const input = 2;
    expect(makeBooleans(input)).toEqual([true]);
  });
});

describe ('idPrimeNumbers', () => {
 
  test('', () =>{

  });
});